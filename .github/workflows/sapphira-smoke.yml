name: sapphira-smoke

# Phase 5 verification — runs the 29-test battery against a checkout of
# sapphira-clinic. Triggered manually for now; will become a scheduled
# job once v0.1.0 ships.
#
# Status: SKELETON — fully wired up in Phase 5.

on:
  workflow_dispatch:
    inputs:
      sapphira-ref:
        description: "sapphira-clinic git ref to test against"
        default: "feat/feedback-widget"
        required: true

jobs:
  battery:
    runs-on: ubuntu-latest
    if: false  # disabled until Phase 5 lands
    steps:
      - run: echo "TODO: see docs/INSTALL-SAPPHIRA.md and tests/sapphira-e2e/"
