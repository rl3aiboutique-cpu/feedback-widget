{
  "name": "rl3-feedback-widget-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "RL3 Feedback Widget — extractable in-app feedback module for any web host.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r --filter @rl3/feedback-widget build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
